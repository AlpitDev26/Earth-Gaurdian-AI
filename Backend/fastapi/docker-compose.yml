version: '3.8'

services:
  fastapi-ai:
    build: .
    ports:
      - "8000:8000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - SPRING_BOOT_URL=http://springboot-core:8080/api/v1
    networks:
      - earth-guardian-net

networks:
  earth-guardian-net:
    driver: bridge
